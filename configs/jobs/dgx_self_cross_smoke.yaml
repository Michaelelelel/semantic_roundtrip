schema_version: 2

job:
  name: dgx_self_cross_smoke
  output_directory: runs
  continue_on_error: false

experiments:
  - name: self_description_route
    config: ../experiments/dgx_self_smoke.yaml

  - name: cross_description_route
    config: ../experiments/dgx_smoke.yaml
