# Local user that owns run artifacts and ComfyUI runtime files.
# Find these values with: id -u && id -g
HOST_UID=
HOST_GID=

# Machine-local storage roots. Absolute paths.
MODEL_ROOT=
RUN_ROOT=
RUNTIME_ROOT=
